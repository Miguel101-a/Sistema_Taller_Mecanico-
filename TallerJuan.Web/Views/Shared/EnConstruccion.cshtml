@model string
@{
    ViewData["Title"] = Model;
}

<div class="p-5 mb-4 bg-white rounded-3 shadow-sm border text-center">
    <h1 class="h3 fw-semibold mb-3">Módulo de @Model</h1>
    <p class="fs-5 text-muted mb-0">🚧 En construcción</p>
    <p class="text-muted mt-2 mb-0">Esta funcionalidad se implementará en una fase posterior.</p>
</div>
